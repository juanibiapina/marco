- Add quoting syntax
- Add Modules
- Add lists
- Numbers
  - Floating point
  - Fractions
  - Scientific notation
- Strings as lists?
- Pair syntax ?
- Add or
- Add and


- Examples

    (def list (function arg arg))
    (def list? (function (l) (or (nil? l) (and (pair? l) (list? (second l))))))
    (def length (function (l) (if (nil? l) 0 (+ 1 (length (tail l))))))
    (def head first)
    (def tail second)