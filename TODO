- Add quoting syntax
- Modules
- Numbers
  - Floating point
  - Fractions
  - Scientific notation
- Strings as lists?
- Pair syntax ?
- Variadic macros
- Better error messages
- Line number information



- Can be implemented

(def head first)
(def tail second)

(def list (function arg arg))
(def length (function (l) (if (nil? l) 0 (+ 1 (length (tail l))))))


- Need more features

(def list? (function (l) (or (nil? l) (and (pair? l) (list? (second l))))))
(def and // variadic macro
(def or // variadic macro